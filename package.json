{
  "name": "adamhacker-website",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "preview": "nuxt preview",
    "deploy": "npm run build && gh-pages -d .output/public --dotfiles"
  },
  "dependencies": {
    "nuxt": "^3.17.6"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0"
  }
}
